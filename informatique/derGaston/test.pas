program gastonTest;
var
	x:integer; (*3dad el marrat*)
	s:string; (*Elli besh n3awdouh*)
BEGIN
writeln('het shttheb t3awed');
readln(s);
writeln('9addesh men marra theb t3awed?');
readln(x);
while(x<>0) do
	begin
		writeln(s);
		x:=x-1
	end;
END.
